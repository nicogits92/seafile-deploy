# =============================================================================
# ccnet.conf — Seafile internal networking (DEPRECATED)
# =============================================================================
# Written by:  Seafile container on first boot (auto-generated)
# Deployed to: $SEAFILE_VOLUME/seafile/conf/ccnet.conf
#
# THIS FILE SHOULD NOT EXIST in a Seafile 13 Docker deployment.
#
# ccnet.conf was deprecated in Seafile 11→12. All configuration it previously
# contained is now handled via environment variables in .env and injected into
# the containers by docker-compose. The official Seafile upgrade docs for 11→12
# say to remove the entire file.
#
# Seafile 13 may still auto-generate this file on first boot as a legacy
# artefact. seafile-config-fixes.sh detects and removes it automatically by
# checking for the [General] section header that Seafile writes.
#
# If you see this file in your $SEAFILE_VOLUME/seafile/conf/ directory, remove
# it manually or re-run seafile-config-fixes.sh.
#
# Below is what Seafile typically auto-generates, for reference only:
# =============================================================================

[General]
# Used internally by Seafile. Do not edit.
ID=<auto-generated-unique-id>
NAME=<your-server-name>

[Network]
# No longer used in Seafile 12+.
PORT=10001

[Client]
# No longer used in Seafile 12+.
PORT=13419
